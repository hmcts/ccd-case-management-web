let StepUtils = require('../../../utils/step.utils.js')
let CaseCreateStep = require('../../../page-objects/caseCreateStep.po.js')
let CaseView = require('../../../page-objects/caseView.po.js')

describe('Caseview - basic / simple inputs', function() {

beforeEach(function(){

});

afterEach(function(){

});

it('Should have case with simple types inputs display');

it('Should have case with simple types inputs (previous) displayed');

it('Should have case with simple types inputs (change answers) displayed');

});
